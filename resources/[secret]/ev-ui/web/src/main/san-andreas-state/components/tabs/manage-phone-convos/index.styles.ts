import { makeStyles } from "@mui/styles";

export default makeStyles({
    detailsWrapper: {
        display: 'flex',
        width: '100%',
        alignItems: 'center',
        justifyContent: 'space-between'
    },
    btnContainer: {
        display: 'flex',
        marginBottom: '0.5rem'
    }
});