import { makeStyles } from '@mui/styles';

export default makeStyles({
    container: {
        height: '100%',
        width: '100%',
        background: '#2f323a'
    },
    number: {
        textAlign: 'center',
        color: 'red',
        marginTop: '1rem'
    },
    form: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100%',
        gap: '1rem'
    }
});