import { makeStyles } from '@mui/styles';

export default makeStyles({
    root: {
        display: 'flex',
        justifyContent: 'start',
        width: '100%',
        paddingLeft: 0
    },
    imageWrapper: {
        paddingLeft: '2rem',
        width: '100%'
    },
    image: {
        width: '25rem'
    }
});