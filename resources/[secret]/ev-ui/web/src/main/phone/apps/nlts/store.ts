import { StoreObject } from "lib/redux";

const store: StoreObject = {
    key: "phone.apps.nlts",
    initialState: {
        drivers: [],
        page: 'home',
        isEmployee: false,
    }
};

export default store;