import { makeStyles } from '@mui/styles';

export default makeStyles({
    footer: {
        bottom: 0,
        right: 0,
        padding: 10,
        width: '100%',
        display: 'flex',
        position: 'fixed',
        alignItems: 'center',
        background: '#31394d',
        justifyContent: 'space-between',
        boxShadow: '0 -4px 4px 0px #00000024'
    }
});