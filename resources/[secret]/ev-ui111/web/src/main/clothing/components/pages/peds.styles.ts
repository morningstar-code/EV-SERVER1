import { makeStyles } from "@mui/styles";

export default makeStyles({
    container: {
        pointerEvents: 'all'
    },
    paperContainer: {
        width: '100%',
        marginTop: '7.5%',
        borderBottom: 'none'
    },
    innerContainer:  {
        padding: 12
    }
});