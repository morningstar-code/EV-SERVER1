const suspensions: { first_name: string, last_name: string, suspension_end: number, suspended_by: string }[] = [];

export const getCurrentSuspensions = async (pSource: number) => {
    return suspensions;
};